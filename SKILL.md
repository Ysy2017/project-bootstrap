---
name: project-bootstrap
description: 用「规划优先 + 分步实现」方法论启动新项目、续做半成品或重构。用户说"开个新项目/新模块"、"搭项目脚手架"、"用 vibe coding 流程做 X"、"把 idea 落成可实现的计划"、"继续做这个项目"、"封存/开新对话"时触发。蒸馏自 EnzeD/vibe-coding,改用 Claude Code 原生能力(subagent / plan mode / hook / 落盘可弃对话)替代手动仪式。
---

# project-bootstrap — 规划优先、分步实现的项目脚手架

## 用途与适用门槛

把模糊 idea / 半成品 / 待重构,变成可执行的规划文档 + 一条「一步一验证」的实现流水线。核心理念三条:

1. **规划优先**:不让 AI 自主乱规划(EnzeD 原则)。
2. **状态落盘、对话可弃**:真相在 `progress.md`/`architecture.md` 等文件里,不在上下文窗口里——所以对话随时能扔、能换。
3. **子代理隔离**:每步的重活在一次性子代理里干,只回窄摘要,主线不被污染。

**何时别用**:一行小改、临时脚本、单文件 bugfix —— 上整套(澄清→文档→子代理→门)是杀鸡用牛刀。多步骤、跨文件、需要留档的工作才值得。

---

## 产物约定:5 份职能文档

`memory_bank` 是**角色名**,不是必须叫这名的文件夹。需要这几份**职能**(谁来扮演看下文):

```
PRD.md                  # 要做什么、给谁、成功标准。不写技术
tech-stack.md           # 技术选型 + 为什么(最简但稳健)
implementation-plan.md  # 分步计划:每步小而具体、带「验证标准」、不写代码
progress.md             # 滚动:每步做完写一条 + 末尾「未整理决策」草稿区
architecture.md         # 活文档:每个文件/模块干什么、数据结构/schema
```

### 文档清单(路径映射)—— 入口时建立一次

**已有项目的文档常分散、且文件名各异**(PRD 在根、架构信息可能内嵌在某 NOTES 里)。所以不假设它们在同一个 `<docs>/` 目录,而是**建一张「职能→实际路径」映射**,写进 `CLAUDE.md` 一次,之后每个会话/子代理都按它取路径:

```
PRD   → <实际文件>      架构 → <实际文件或某文件的某节>
计划  → <实际文件>      进度 → <实际文件>      决策 → <实际文件>
```

### 放置规则(缺的文件新建在哪)

- **根目录已有同类文档**(如本仓库 CONTEXT.md / DECISIONS.md / PRD)→ 缺的文件**就近放根**。**不建 memory_bank/、不搬现有文件**(搬会断十几处 md 链接 + 让满项目源码注释里的 "DECISIONS.md" 名不副实)。
- **根目录没有**等价文档(空目录/纯代码)→ **新建 `memory_bank/` 文件夹**归拢。
- 一句话:**跟现有文档同处;没有才建 memory_bank/。**

---

## 入口:判断模式(每次触发先做)

从用户话里**自动推断**模式,**拿不准才问**;扫描**只限当前项目根**(别跑进其它工作目录如 wiki 库)。

- **新建**(空目录/只有 idea)→ 走完整 Phase 0→4。
- **续做**(已有代码、做一半)→ 走「续做对接」→ 进 Phase 3。
- **重构**(改现有代码、不能破坏现有行为)→ 「续做对接」+ **锁基线**:跑现有测试记下全绿;关键路径无测试就先补特征测试(characterization tests)锁住当前行为;之后每步都回归全绿。

### 续做对接(续做/重构专用)

1. **检测现有文档(按职能,不按文件名)**:读内容判断谁在扮演 PRD/架构/进度等。**拿不准就问**用户"CONTEXT.md 是不是充当架构+进度?"。建好上面的「文档清单」。
2. **只补真缺的**:判"缺不缺"看**信息在不在**,不是文件名在不在。架构信息若已内嵌在某文档里 → **指过去,别另造 architecture.md**(否则重复+漂移)。真没有才新建(常缺 architecture.md / progress.md,可据现有代码反向生成,经用户确认)。
3. **整理剩余步骤**:把没做的工作收敛成 implementation-plan 的剩余步骤(用户有清单就直接转成带验证标准的分步)。**只列没做的,别重排已完成的。**
4. **确保 Always 规则存在**:检查 CLAUDE.md 有没有 Phase 2 那套规则,没有就补。
5. **进 Phase 3 循环。**

---

## 工作流(新建走 Phase 0→4;续做/重构经对接后进 Phase 3)

> Phase 2(Always 规则)、Phase 4(增强)**三模式通用**,非新建专属。下文 `<职能>` 路径一律按「文档清单」解析,写进真实文件时替换成实际路径。

### Phase 0 — 澄清(别急着生成)
向用户提 5~9 个关键问题(目标用户、核心 1~2 功能、平台、约束、明确不做什么)。**等回答后**再进 Phase 1。

### Phase 1 — 生成规划文档
生成 PRD、tech-stack(指令:*"给最简但最稳健的栈,每项说明为什么、否决了什么更复杂的替代"*),再生成 implementation-plan:
- 只覆盖 MVP;每步小到「一个子代理一轮能做完」;**每步带一行验证标准**;不写代码。
- progress / architecture 先占位。生成后让用户过一遍计划:"哪步我可能理解有偏差?"

### Phase 2 — CLAUDE.md Always 规则(写入时替换 `<职能>` 为实际路径)
```markdown
## 项目规则(每次写码前遵守)
- 写任何代码前,先读 <架构文档>(含数据结构/schema)和 <PRD>。
- 倾向模块化:多个小文件 > 单个巨石文件。
- 完成里程碑/大功能后,更新 <架构文档> 和 <进度文档>。
- 改外部配置字段时,主动提示用户填到 .env。
```
> 有可自动化的检查(lint/test/typecheck)→ 建议用 **hook**(PostToolUse/Stop)强制,而非靠记得。需要时引导用户用 update-config skill 配。

### Phase 3 — 分步实现循环(每步重复)
> 开始本循环前确保已在里程碑分支上(何时开 / 命名 / 何时合见下「**分支:何时开 / 何时合**」节)。
1. **派子代理实现**:*"读全部规划文档(按文档清单)。实现 Step N,**只做这一步**。改完先不要跑测试,把改了哪些文件、关键决策、打算如何验证,**外加按自审清单的自查疑点**,回报给我——只回结构化短摘要,不贴整文件、不贴长日志。"*
2. **人工确认门**:把子代理的改动摘要 + 自审疑点 + 验证计划呈现给用户,**问是否同意跑测试**,等点头。门的意义是**你真读了 diff**,不是橡皮图章。
3. **跑验证**:同意后执行验证标准。失败 → 错误交回子代理修;通过 → 下一步。**"测试通过"必须附真实运行输出。**
4. **收尾**:`git commit`(含 Step N 摘要);更新 progress / architecture;把本步「未整理决策」整理进决策文档;进 Step N+1(新子代理)。

**自审清单**(仅**实质性步**做——碰共享代码/新逻辑/多文件/改签名/数据完整性;琐碎步如 1 行、注释、文案跳过)。结果**并进步骤 1 的汇报**,让人在门口同时看到 diff + 疑点。**只自查、不跑 `/code-review`**(真审查留到里程碑,见「换会话」节):
1. **遗漏**:对照本步目标,有没有漏做
2. **逻辑错**
3. **未覆盖的边界**
4. **假设核验**:依赖的"X 没人用 / memory / 文档说的"核实了吗(别信旧标注)
5. **爆炸半径**:改的东西谁还在用——删/改签名前 grep 调用方
6. **不变量**:文档锁定的不变量动了没(如某字段永不变、两实现行为对齐)
7. **同类一致性**:做同件事的 N 条路径,这条和其它一致吗

> 诚实边界:自审抓**正确性/边界**类;**抓不住**缺工具、LLM 行为、深层模块交互这类——那些只有**真用/手测**能逼出。**自审/review 都不替代手测。**

不一次推进多步;用户验证前不开始下一步。

### Phase 4 — 增强与打磨
每个增强单独建 `feature-<name>` 文档(与其它规划文档同处),写小步骤 + 验证标准,走 Phase 3 同款循环。

---

## 护栏(AI 编码特有翻车点,每步都盯)

- **越权修改**:只改本步相关的;**不顺手重构/重命名/删无关代码**;要连带改先报告,不擅自动手。
- **假绿/谎报**:"测试通过"必须附真实输出;警惕 `assert True` 式空测试——看测试是否真覆盖行为。
- **幻觉/膨胀**:用到的库/API 先确认真实存在;加新依赖要有理由、最简优先。
- **密钥卫生**:不硬编码密钥;首次 commit 前确认 `.gitignore` 盖住 `.env`/凭证;commit 前扫一眼提交了什么。
- **回归盲区**:别只跑本步测试,周期性跑全量,防旧功能悄悄坏。
- **计划可能错**:规划优先 ≠ 规划永远对;实现中发现计划有误,**改计划并同步文档**,别硬按错计划走、让代码与 plan 偷偷分叉。

---

## 上下文管理(对话可弃的前提)

- **状态落盘 → 对话可弃**:任何时候上下文重了,`commit` + 更新 progress,然后 `/clear` 或开新会话,从文档续上,零损失。
- **阈值习惯**:`/context` 低于 ~35% 就在干净边界 checkpoint-and-clear,别硬撑。
- **子代理返回要窄**:重活留在子代理上下文里消化,只回结论;长日志/整文件/堆栈别灌进主线,失败只留那一两行断言。
- **`/compact` vs `/clear`**:想继续当前这步但重了 → `/compact`;步与步之间 → 直接 `/clear`/开新会话。
- **architecture.md 质量 = 重启成本**:一份好架构文档让重开后读一份就懂全局,省掉重新 grep 全仓库。保持它真实,既是文档卫生也是上下文地基。

---

## 当场落盘(让记录不打断节奏)

**捕获 ≠ 整理**,拆开就不卡心流:

- **捕获(连续、零成本,agent 自动做)**:每做出/确认一个决定(改了计划/否决方案/发现坑/定约定),agent **顺手 append 一行**到 `progress.md` 末尾「未整理决策」区——不问、不等、不格式化。门槛:只记"影响以后/别的模块/否决替代/约束或坑"的,纯实现细节不记。
- **整理(批量,对齐已有边界)**:在 Phase 3 每步收尾 / 封存时(本来就要停),把草稿行提炼进决策文档、编号、清空草稿。
- 主力是**当场捕获**,封存时的扫描只是网——因为长会话会 compaction,攒到最后早期决定可能已丢。

---

## 换会话:封存 / 恢复

判据不是"状态完美",而是:**只凭盘上文件 + git,能否让没参与本会话的 agent 无歧义重建当前状态?** (红测试/脏工作区也能封存——只要被精确记录。)

### 主动提示封存的时机(我开口,不等用户说)
封存默认被动(用户说"封存"才跑),但下面这些**干净边界**我该**主动问一句**「这是干净 checkpoint,要封存 / 开新会话吗?」——尤其本会话已 compact 过、或明显很长时:
- **里程碑 / 一组相关 step 做完,准备合并或 push** —— 配合「里程碑合并前 review」一节:review→分诊→合并→push 走完那一刻,上下文最满又最干净,最该提。
- **到干净边界、要转去做不相关的另一件事**。
- **当场落盘没跟上、盘上已落后于对话里定的决定**(sweep 该补了)。
- **用户说要 `/clear` / 开新会话** —— 最该**先拦**:`/clear` 丢上下文不可逆,先封存再 clear。

**不提**:每步收尾(太碎——每步只 commit + 更新 progress)、卡在某步中途且脏/红(先收口到干净边界再说)。
**注**:上下文占比的阈值我**感知不准**,那条靠用户自己看 `/context`(见「上下文管理」§阈值习惯),不靠我主动判断。

### 封存前置闸(用户说"封存/要开新对话",或上面主动提示被接受时跑;任一不过 → 拒绝并给补救)
| 阻断条件 | 能否自测 | 补救 |
|---|---|---|
| 卡在某步中途 | 部分 | 做到绿 / `git reset` 回上个 commit / 精确写下停在哪 |
| 工作区有未提交改动 | ✅ `git status` | commit;或注明故意不提交+原因 |
| 测试红且没记录 | ✅(若跑了) | 修绿;或记"已知失败 X/原因/下一步" |
| 进度文档 ↔ git/工作区 对不上 | ✅ 对账 | 先 reconcile 到一致 |
| 光标缺失(说不清下一步) | ✅ | 补"下一步 = Step N+1: …" |
| 不可逆外部操作没收尾(迁移/部署/外部状态) | ⚠️ 多半要问 | 完成或回滚,并记下其状态 |
| 口头决定没落盘 | 🟡 半自动 | 见下方 sweep |

**光标**写进项目既有的交接文档(本仓库 = `CONTEXT.md`,它本就声明"新对话先读我"),别另起 HANDOFF.md。

### 半自动 sweep(查漏掉的口头决定)
封存时整段对话还在上下文里,所以能:① 扫上下文里的决策信号(改用/不用/其实/放弃/发现…坑/约定);② diff 自上次更新文档以来的代码,捞"只在代码里、没在文档里"的隐性决定;③ 与盘上决策文档对账 → 出**「疑似未落盘」候选清单**(附建议写到哪),念给用户确认。
> 洞(诚实):只覆盖**还在窗口里**的——compaction 压掉的早期决定、跨会话遗留、纯动机,扫不全。所以靠「当场落盘」当主、sweep 当网。

### 恢复仪式(新对话说"继续"时)
1. 检测=续做模式,读文档清单里各文档 + `git log` + `git status`。
2. **三方对账**:progress 说的"最后做完" ↔ git 最后 commit ↔ 工作区是否 clean。**对不上就停、报给用户**,不盲目往下。
3. **念光标给用户确认**:"上次到 Step 7 已验证,接下来 Step 8 = X,对吗?" 点头才续。
4. 进 Phase 3。

---

## 分支:何时开 / 何时合(分支 = 里程碑,三边界对齐)

skill 已定义两层颗粒度:**步** = 一次 commit、**里程碑** = 合 master + review。中间的**分支层**这样填:**一个里程碑一条分支**,让三个边界重合成一条链——

> 里程碑开始 → 从 master 开分支 → 每步 commit(Phase 3)→ 里程碑做完 → review(`branch vs master`)→ 合 master → 提封存

这条链不是新规则,是把已有的「里程碑合并前 review」节(审 `branch vs master`)和「封存」节(合并/push 完那刻最该封存)接上分支这根轴。

### 何时开(按入口三模式,起点不同)
- **新建**:Phase 1 出完计划、动第一个里程碑前,从 master 开。
- **续做**:大概率已在分支上 → 恢复对账时确认「在分支、不在 master」即可,别在 master 直接写。
- **重构**:锁基线那步就在分支上做,master 始终保持「已知全绿」基线。
- 通用铁律:**不在 master / 主干上直接写**。

### 分支命名:有就照用,没有当场建一条(别让 agent 瞎编)
命名是**项目级事实,放 CLAUDE.md,不写死在 skill**——否则通用 skill 跑到新项目、CLAUDE.md 没这条时,agent 会自己瞎编名字 → 不一致、漂移。开分支前:
- **CLAUDE.md 有规则 → 照用**(哪怕其颗粒度不完全是 feature,尊重既有事实,**别覆盖**)。
- **没有 → 和用户定一条、写进 CLAUDE.md**(可建议 `feature-<kebab>`,仅建议、用户拍板)。落进 CLAUDE.md 后项目层可见可查,之后每会话 / 子代理照用。
> 这和入口「确保 Always 规则存在,没有就补」、文档清单「写进 CLAUDE.md 一次之后照取」**同构**——又一条「建立一次、落盘、照用」的约定,只在「第一次且确实缺」时问一句,之后零打断。

### 优先级:CLAUDE.md 是事实,skill 是倡议
「一个里程碑一条分支、做完即合」是 skill 的**倡议层**;**命名与颗粒度的事实以 CLAUDE.md 为准**(如某项目用日期分支 `ysy<MMDD>`,**不被 skill 覆盖**)。冲突时照 CLAUDE.md,靠纪律尽量让「一个分支只装一个里程碑」。

### 何时合
触发 = 里程碑完成 + review 过 + 重测全绿(细节见下节)。要点:
- **合并 = 这条分支的终点**;合完即到最干净边界 → 主动提封存。
- **做完即合,别攒**:分支活太久会和 master 漂移 → 合并冲突 + review diff 变大变脏。

---

## 里程碑合并前:独立 review(软提醒)

**触发**:某里程碑做完、分支准备**合 master / push** 时(**不是每步**——每步只做 Phase 3 的自审清单)。
**前提**:先 commit、工作区干净——分支级 review 审的是 `branch vs master`(已提交干净正是标准状态;不需 push/PR/远程)。

满足时**提醒用户**(软提醒,给两档、默认不花钱):

> "这条分支 N 个 commit 要合并了,建议**新开一个对话窗口**审一遍:
> 省钱档 `/code-review high`(本地、独立干净视角,够用);高风险才上 `/code-review ultra`(云端多 agent,**计费**)。"

要点:
- **新开窗口、别 fork**:零上下文才"干净"(fork 会带本对话的实现偏向)。review 读的是 git diff,新对话同分支即可。
- **手动接力**:开窗口、跑、把 findings 贴回来都由用户做——skill 接不到另一对话输出,`ultra` 也只能用户触发(计费)。
- review 那边**只出 findings、别 `--fix`**:避免和实现窗口同时写文件打架;findings 拿回本对话改。
- **回来后**:分诊(真 bug 修 / 误报丢,**别无脑 --fix**)→ 改 → **重测** → 全过才合并。
- **合并 / push 完 → 主动提一句封存**:这条 review→合并→push 链走到末端,正是上下文最满又最干净的换会话点;别止于 review,接着问「要封存 / 开新会话吗」。触发判据与不提的情形见「换会话:封存 / 恢复」§主动提示封存的时机。
- (可选)progress/CONTEXT 记一行"已在 commit Y review 过",防重复、换会话也知道状态。
- **诚实边界**:review 抓正确性/边界类;缺工具、LLM 行为、深层交互这类仍需**手测**,review 不替代手测。

---

## 卡住时的恢复
- **某步反复失败**:`git reset` 回上个 commit,改 prompt 重来。频繁提交是安全网。
- **要深推理**:对子代理用 `think` < `think hard` < `think harder` < `ultrathink`;或加"想多久都行,我不急,关键是做对、按我说的精确执行,不清楚就问我"。
- **规划不确定**:先进 Plan Mode(Shift+Tab)出方案,确认后再写。

## 与原指南的差异
| 原指南(手动时代) | 本 Skill(Claude Code 原生) |
|---|---|
| 每步手动 `/clear` 隔离上下文 | 每步一个 subagent,天然隔离 |
| "我来跑测试、你等我"纯人工 | 子代理实现 → 人确认 → 跑验证 |
| RepoPrompt/uithub 塞单文件诊断 | 原生读全仓库,已不需要 |
| memory-bank 五件套硬造 | 已有等价文档复用、不重造 |
| (无)上下文/换会话方案 | 状态落盘→对话可弃 + 封存/恢复双闸 |
| (无)审查机制 | 每步自审清单(并进汇报门)+ 里程碑合并前独立 /code-review |
| (无)分支生命周期 | 分支 = 里程碑,三边界(分支 / review / 封存)对齐;命名缺则当场建并落 CLAUDE.md |
